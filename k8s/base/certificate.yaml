apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tasks-app
  namespace: tasks-app
spec:
  secretName: tasks-app-tls
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned-cluster-issuer
  commonName: tasks.home.arpa
  dnsNames:
    - tasks.home.arpa

